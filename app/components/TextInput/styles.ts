import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    text: {
        color: 'black',
        fontSize: 15,
        marginHorizontal: 20,
    },
    input:{
        marginHorizontal: 20,
    }

})

export default styles;
